import type { Metadata } from "next";

export const metadata: Metadata = { title: "Connect eBay" };

export default function ConnectEbayLayout({ children }: { children: React.ReactNode }) {
  return children;
}
